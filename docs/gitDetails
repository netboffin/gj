Global setup:
 Set up git
  git config --global user.name "Michael"
  git config --global user.email projects@netboffin.com
      
Next steps:
  mkdir gj
  cd gj
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:netboffin/gj.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:netboffin/gj.git
  git push -u origin master
      
Importing a Subversion Repo?
  Click here
      
When you're done:
  Continue
